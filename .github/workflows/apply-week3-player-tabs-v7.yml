name: Apply Week 3 player tabs v7

on:
  push:
    branches:
      - main
    paths:
      - week3-player-tabs-v7.js
      - api/sle-roster.js
      - api/sle-player-image.js
      - .github/workflows/apply-week3-player-tabs-v7.yml

permissions:
  contents: write

jobs:
  patch-index:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Insert Week 3 v7 player tabs after v6
        run: |
          python - <<'PY'
          from pathlib import Path
          import re
          p=Path('index.html')
          s=p.read_text()
          tag='<script src="./week3-player-tabs-v7.js?v=20260912e"></script>'
          pat=r'<script src="\.\/week3-player-tabs-v7\.js\?v=[^"]+"></script>'
          if re.search(pat,s):
              s=re.sub(pat,tag,s)
          elif tag not in s:
              anchor=re.search(r'<script src="\.\/week3-player-fix-v6\.js\?v=[^"]+"></script>',s)
              if anchor:
                  s=s[:anchor.end()]+'\n'+tag+s[anchor.end():]
              elif '</body>' in s:
                  s=s.replace('</body>',tag+'\n</body>')
              else:
                  raise SystemExit('No insertion point found')
          p.write_text(s)
          PY
      - name: Commit patched index
        run: |
          if git diff --quiet -- index.html; then
            echo "index already current"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add index.html
          git commit -m "Load Week 3 player tabs v7"
          git push
